import {Role}       from '../roles/role';
import {BaseDomain} from '../shared/components/base-domain.component';

export class ComponentURI extends BaseDomain{
      compUriId : string;
      uri : string;
      role : Role;
      compId : string;
}